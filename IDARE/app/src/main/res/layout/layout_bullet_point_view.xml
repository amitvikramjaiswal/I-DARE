<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewModel"
            type="com.opensource.app.idare.viewmodel.BulletPointViewModel" />
    </data>

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/padding_half"
        android:paddingTop="@dimen/padding_half"
        android:text="@{viewModel.observableBulletPoint}"
        android:textColor="@color/idare_nav_drawer_background"
        android:textSize="@dimen/font_size_medium_minus_1" />
</layout>